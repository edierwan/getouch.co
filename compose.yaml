services:
  caddy:
    image: caddy:2-alpine
    container_name: caddy
    restart: unless-stopped
    depends_on:
      - web
      - wa
      - pgadmin
      - open-webui
      - searxng
    environment:
      ADMIN_AUTH_USER: ${ADMIN_AUTH_USER:-getouchadmin}
      ADMIN_AUTH_HASH: ${ADMIN_AUTH_HASH}
    ports:
      - 127.0.0.1:80:80
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider --header='Host: getouch.co' http://127.0.0.1/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
    volumes:
      - ./infra/Caddyfile:/etc/caddy/Caddyfile:ro
      - ./infra/static:/etc/caddy/static:ro
      - /data/getouch/caddy/data:/data
      - /data/getouch/caddy/config:/config
    networks:
      - edge
      - coolify

  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: getouch-web
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://127.0.0.1:3000/ || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
    networks:
      - edge

  wa:
    build:
      context: ./services/wa
      dockerfile: Dockerfile
    container_name: getouch-wa
    restart: unless-stopped
    environment:
      PORT: ${WA_PORT:-3001}
      WA_API_KEY: ${WA_API_KEY}
      WA_AUTH_DIR: /app/data/auth
      LOG_LEVEL: ${WA_LOG_LEVEL:-info}
    volumes:
      - /data/getouch/wa:/app/data
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:${WA_PORT:-3001}/healthz | grep -q '\"status\":\"ok\"' || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
    networks:
      - edge

  postgres:
    image: postgres:16-alpine
    container_name: getouch-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${APP_DB_NAME:-getouch}
      POSTGRES_USER: ${APP_DB_USER:-getouch}
      POSTGRES_PASSWORD: ${APP_DB_PASSWORD}
    volumes:
      - /data/getouch/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${APP_DB_USER:-getouch} -d ${APP_DB_NAME:-getouch}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - edge
      - coolify

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: getouch-pgadmin
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_DEFAULT_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_DEFAULT_PASSWORD}
      PGADMIN_DISABLE_POSTFIX: "true"
      PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED: "False"
      PGADMIN_CONFIG_ENABLE_SERVER_PASS_EXEC_CMD: "True"
      PGADMIN_LISTEN_PORT: "80"
    volumes:
      - /data/getouch/pgadmin:/var/lib/pgadmin
      - ./infra/pgadmin/servers.json:/pgadmin4/servers.json:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1/misc/ping | grep -q PING || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
    networks:
      - edge

  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    restart: unless-stopped
    runtime: nvidia
    environment:
      NVIDIA_VISIBLE_DEVICES: all
      OLLAMA_KEEP_ALIVE: "-1"
      OLLAMA_MAX_LOADED_MODELS: "1"
      OLLAMA_NUM_PARALLEL: "1"
    volumes:
      - /srv/apps/ai/ollama:/root/.ollama
      - /srv/apps/ai/models:/models
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - edge

  open-webui:
    image: ghcr.io/open-webui/open-webui:v0.8.12
    container_name: open-webui
    restart: unless-stopped
    depends_on:
      ollama:
        condition: service_healthy
      postgres:
        condition: service_healthy
      searxng:
        condition: service_healthy
    environment:
      OLLAMA_BASE_URL: http://ollama:11434
      DATABASE_URL: postgresql://${APP_DB_USER:-getouch}:${APP_DB_PASSWORD}@postgres:5432/open_webui
      WEBUI_SECRET_KEY: ${WEBUI_SECRET_KEY:-changeme-in-production}
      WEBUI_NAME: Getouch Chat
      ENABLE_SIGNUP: "true"
      ENABLE_IMAGE_GENERATION: "false"

      # ── RAG Web Search via SearXNG ──
      ENABLE_RAG_WEB_SEARCH: "true"
      RAG_WEB_SEARCH_ENGINE: "searxng"
      SEARXNG_QUERY_URL: "http://searxng:8080/search?q=<query>"
      RAG_WEB_SEARCH_RESULT_COUNT: "5"
      RAG_WEB_SEARCH_CONCURRENT_REQUESTS: "5"

      # ── RAG Embeddings ──
      RAG_EMBEDDING_ENGINE: "ollama"
      RAG_EMBEDDING_MODEL: "all-minilm:latest"
      RAG_OLLAMA_BASE_URL: http://ollama:11434
      RAG_PDF_EXTRACT_IMAGES: "true"

      # ── Pipelines connection ──
      OPENAI_API_BASE_URLS: "http://pipelines:9099;https://api.openai.com/v1"
      OPENAI_API_KEYS: "0p3n-w3bu!;${OPENAI_API_KEY:-sk-placeholder}"

      # ── Features ──
      ENABLE_ADMIN_EXPORT: "true"
      ENABLE_COMMUNITY_SHARING: "true"
      DEFAULT_MODELS: "getouch-smart-assistant"
    command:
      - bash
      - -c
      - |
        if [ -f /app/build/index.html ] && ! grep -q 'getouch-custom-css' /app/build/index.html; then
          sed -i 's|</head>|<style id="getouch-custom-css">.assistant-message-profile-image{width:1.5rem!important;height:1.5rem!important}[id=response-message-model-name]{display:none!important}form img[src^="data:image"],form img[src*="/api/v1/files/"],form img[src*="/api/v1/attachments/"]{max-width:min(50vw,420px)!important;max-height:240px!important;width:auto!important;height:auto!important;object-fit:contain!important;border-radius:12px!important;border:1px solid rgba(255,255,255,.12)!important}form [class*="attachment"],form [class*="upload"],form [class*="image"]{min-height:88px}form [class*="attachment"]:has(img),form [class*="upload"]:has(img){display:flex!important;align-items:flex-start!important;gap:8px!important;flex-wrap:wrap!important}.prose table:has(th img){border:none!important;display:block!important;overflow-x:auto!important;width:100%!important;scrollbar-width:thin!important;padding:8px 0 4px!important}.prose table:has(th img) thead,.prose table:has(th img) tbody{display:contents!important}.prose table:has(th img) tr{display:flex!important;gap:16px!important}.prose table:has(th img) th,.prose table:has(th img) td{border:none!important;padding:0!important;flex:0 0 200px!important;width:200px!important;min-width:200px!important;max-width:200px!important;background:transparent!important;box-sizing:border-box!important}.prose table:has(th img) th{height:150px!important;min-height:150px!important;max-height:150px!important;position:relative!important;overflow:hidden!important;border-radius:14px!important;background:#1a1a2e!important;cursor:pointer!important;transition:box-shadow .3s ease!important}.prose table:has(th img) th:hover{box-shadow:0 4px 20px rgba(255,255,255,.1)!important}.prose table:has(th img) th img{position:absolute!important;top:0!important;left:0!important;display:block!important;width:100%!important;height:100%!important;max-width:none!important;max-height:none!important;object-fit:cover!important;border-radius:0!important;margin:0!important;transition:transform .3s ease!important}.prose table:has(th img) th:hover img{transform:scale(1.05)!important}.prose table:has(th img) td{display:-webkit-box!important;-webkit-line-clamp:2!important;-webkit-box-orient:vertical!important;overflow:hidden!important;font-size:13px!important;font-weight:600!important;line-height:1.3!important;min-height:48px!important;padding-top:8px!important;color:#e0e0e0!important;text-align:center!important}</style></head>|' /app/build/index.html
        fi
        exec bash start.sh
    volumes:
      - /srv/apps/ai/open-webui:/app/backend/data
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://127.0.0.1:8080/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - edge

  # ────────────────────────────────────────────────────────
  # SearXNG — privacy-respecting meta search engine
  # ────────────────────────────────────────────────────────
  searxng:
    image: searxng/searxng:latest
    container_name: searxng
    restart: unless-stopped
    environment:
      SEARXNG_SECRET: ${SEARXNG_SECRET:-change-me-in-production}
    volumes:
      - ./infra/searxng/settings.yml:/etc/searxng/settings.yml:ro
      - ./infra/searxng/uwsgi.ini:/etc/searxng/uwsgi.ini:ro
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/healthz || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
    networks:
      - edge

  # ────────────────────────────────────────────────────────
  # Open WebUI Pipelines — extensible AI middleware
  # ────────────────────────────────────────────────────────
  pipelines:
    image: ghcr.io/open-webui/pipelines:main
    container_name: open-webui-pipelines
    restart: unless-stopped
    environment:
      PIPELINES_API_KEY: "0p3n-w3bu!"
    volumes:
      - /srv/apps/ai/pipelines:/app/pipelines
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen(urllib.request.Request('http://127.0.0.1:9099/v1/models', headers={'Authorization':'Bearer 0p3n-w3bu!'}))\" || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - edge

  # ────────────────────────────────────────────────────────
  # SeaweedFS (S3-compatible storage)
  # Master, Volume, Filer and S3 gateway
  # Single-node setup (suitable for development / small deployments)
  # For production consider running multiple volume nodes and masters.
  # ────────────────────────────────────────────────────────
  seaweed-master:
    image: chrislusf/seaweedfs:latest
    container_name: seaweed-master
    restart: unless-stopped
    command: master -port=9333 -mdir=/data -defaultReplication=000
    volumes:
      - /srv/archive/seaweedfs/master:/data
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:9333/cluster/status || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - edge

  seaweed-volume:
    image: chrislusf/seaweedfs:latest
    container_name: seaweed-volume
    restart: unless-stopped
    depends_on:
      seaweed-master:
        condition: service_healthy
    command: volume -port=8080 -ip.bind=0.0.0.0 -dir=/data -max=10 -mserver=seaweed-master:9333 -dataCenter=dc1
    volumes:
      - /srv/archive/seaweedfs/volume:/data
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/status || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - edge

  seaweed-filer:
    image: chrislusf/seaweedfs:latest
    container_name: seaweed-filer
    restart: unless-stopped
    depends_on:
      seaweed-master:
        condition: service_healthy
      seaweed-volume:
        condition: service_healthy
    command: filer -master=seaweed-master:9333 -port=8888 -ip.bind=0.0.0.0
    volumes:
      - /srv/archive/seaweedfs/filer:/data
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8888/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - edge

  seaweed-s3:
    image: chrislusf/seaweedfs:latest
    container_name: seaweed-s3
    restart: unless-stopped
    depends_on:
      seaweed-filer:
        condition: service_healthy
    command: s3 -filer=seaweed-filer:8888 -port=8333 -ip.bind=0.0.0.0 -config=/etc/seaweedfs/s3.json
    volumes:
      - ./infra/seaweedfs/s3.json:/etc/seaweedfs/s3.json:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -qS --spider http://127.0.0.1:8333/ 2>&1 | grep -q '403\\|200' || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
    networks:
      - edge

  filestash:
    image: machines/filestash:latest
    container_name: filestash
    restart: unless-stopped
    environment:
      APPLICATION_URL: s3.getouch.co
    ports: []
    volumes:
      - /data/getouch/filestash:/app/data/state
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8334/ || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 5
    networks:
      - edge

networks:
  edge:
    name: getouch-edge
    driver: bridge
  coolify:
    external: true